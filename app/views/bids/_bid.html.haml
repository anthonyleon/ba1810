%td= bid.company.name
%td= bid.inventory_part.serial_num
%td= bid.inventory_part.condition
%td= bid.inventory_part.manufacturer
%td= number_to_currency bid.amount
%td= bid.average_rating.round(2) || "No Ratings for this User"
- if @auction.company == current_user || bid.company == current_user
  %td= link_to 'Part Documents', inventory_part_path(bid.inventory_part, bid_id: bid.id)
- if bid.company == current_user
  / %td= link_to 'Delete', auction_bid_path(@auction, bid), method: :delete, data: { confirm: 'Are you sure?' }
  %td= link_to 'Delete', auction_bid_path(@auction, bid), data: { auction_bid_behavior: 'delete' }
  / testing purposes
  ///////////// we need an edit bid button 
- elsif @auction.company == current_user
  %td= button_to "Buy Part", {:controller => :auctions, :action => 'purchase', :id => bid.id, :auction_id => @auction.id }, :method => :post
  / %td= link_to 'Buy Part', auction_purchase_path(@auction.id, bid.id)
/ We should put an ARE YOU SURE YOU WANT TO PURCHASE PART_NUM FOR $$ IN CONDITION ??s