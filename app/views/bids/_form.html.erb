<%= form_for(@bid) do |f| %>
  <% if @bid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bid.errors.count, "error") %> prohibited this bid from being saved:</h2>

      <ul>
      <% @bid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :company_id %><br>
    <%= f.text_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :auction_id %><br>
    <%= f.text_field :auction_id %>
  </div>
  <div class="field">
    <%= f.label :inventory_part_id %><br>
    <%= f.text_field :inventory_part_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
